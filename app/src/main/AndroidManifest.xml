<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mastegoane.namazatimes">
    <!-- Remove AD_ID permission added by Firebase Analytics - we don't use advertising features -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application
        android:name=".NamazaTimesApplication"
        android:grantUriPermissions="true"
        android:allowBackup="true"
        android:fullBackupOnly="true"
        android:label="@string/app_name"
        android:icon="@drawable/app_icon_190x190"
        android:roundIcon="@drawable/app_icon_190x190"
        android:supportsRtl="true"
        android:theme="@style/Theme.NamazaTimes">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="Settings" />
        <receiver android:name=".NextPrayerWidgetProvider" android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.mastegoane.namazatimes.ACTION_UPDATE_WIDGET" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider"
                android:resource="@xml/next_prayer_widget_info" />
        </receiver>
    </application>
</manifest>
